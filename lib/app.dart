import 'package:flutter/material.dart';
import 'profile_section.dart';

class App extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: ProfileSection());
  }
}
